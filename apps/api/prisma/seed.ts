/**
 * Demo seed was removed. Data comes from Jira via board sync / hourly cron.
 * This file remains as a no-op entrypoint for `prisma db seed`.
 */
console.log('No seed data. Add a Jira board in the UI and run sync.');
