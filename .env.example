# API
PORT=3001
DATABASE_URL=postgresql://avantpro:avantpro@localhost:5432/avantpro_po?schema=public
REDIS_URL=redis://localhost:6379
WEB_ORIGIN=http://localhost:5173

# Jira Cloud (API token: https://id.atlassian.com/manage-profile/security/api-tokens)
JIRA_HOST=https://grupo-ram.atlassian.net
JIRA_EMAIL=seu-email@empresa.com
JIRA_API_TOKEN=
JIRA_WEBHOOK_SECRET=

# Legacy flag (demo seed removed — keep false)
SEED_DEMO_DATA=false

# Hourly active-issue sync while the API is running (workaround without webhooks)
SYNC_CRON_ENABLED=true

# Web — leave empty to use Vite same-origin proxy (recommended locally)
VITE_API_URL=
VITE_WS_URL=
VITE_API_PROXY_TARGET=http://localhost:3001
